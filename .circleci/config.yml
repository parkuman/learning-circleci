version: 2.1

jobs:
  my-job:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo "Hello, World!"
      - run: exit 1
  my-job2:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo "Hello, World!"

workflows:
  my-badass-workflow:
    jobs:
      - my-job:
          serial-group: my-key
      - my-job2:
          requires:
            - my-job
